class CardReader:
    """Class to manage the Card Reader"""

    def __init__(self, config):
        self.config = config;

    def scanCard(callback, timeout=0):
        # @TODO: arm card reader for <timeout> length (if timeout==0, infinitely until value is read)
        # @TODO: when card is read, fire callback(value)
        pass # do nothing
